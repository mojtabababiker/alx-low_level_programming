#include "main.h"

/**
 * _abs - compute the aps value of int integer
 * @x: the user variable
 * Return: The abs value int
 */

int _abs(int x)
{
	if (x < 0)
	{
		return (-x);
	}
	else
	{
		return (x);
	}
}
