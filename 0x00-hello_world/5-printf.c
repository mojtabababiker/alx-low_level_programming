#include <stdio.h>

/**
 * main - Enty point
 * Return: Always 0 (success)
 * This a basic C program that print a text to the
 * standard output.
 * using the function printf
 */

int main(void)
{
printf("with proper grammar, but the outcome is a piece of art,\n");
return (0);
}
