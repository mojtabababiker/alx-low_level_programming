#include <stdio.h>

/**
 * main - Entry point
 * Return: Always 0 (success)
 * This is simple C program that put to the standerd output
 * a string of hello world.
 * This program use the puts().
 * puts() is a function that print a string value to
 * screen
 **/

int main(void)
{
puts("\"Programming is like building a multilingual puzzle");
return (0);
}
