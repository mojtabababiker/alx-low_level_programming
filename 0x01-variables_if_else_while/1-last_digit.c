#include <stdlip.h>
#include <time.h>
#include <stdio.h>

/**
 * main - enter point
 * Return: Always 0 (success)
 *
 * Program that return the last digit of a number
*/

int main(void)
{
	int n;

	srand(time(0));
	n = rand() - RAND_MAX / 2;

	
