#include "main.h"

/**
 * add - Adding two integer and return the result
 * @x: arg1
 * @y: arg2
 * Return: the sum of the two integer
 */

int add(int x, int y)
{
	int sum;
	sum = x + y
	return (sum);
}
